export enum TodoActions {
    SetTodos = "SET_TODOS",
    AddTodo = "ADD_TODO",
    RemoveTodo = "REMOVE_TODO",
    SetLastRemoved = "SET_LAST_REMOVED",
    RestoreLastRemoved = "RESTORE_LAST_REMOVED",
}
